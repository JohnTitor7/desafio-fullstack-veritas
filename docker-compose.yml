services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend/data:/app/data
    environment:
      - TASKS_FILE=/app/data/tasks.json

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend